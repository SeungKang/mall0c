# this is the builds.sr.ht manifest
image: ubuntu/25.04
sources:
  - https://github.com/SeungKang/mall0c 
tasks:
  - build: |
      cd mall0c
      gcc main.c
      ./a.out | ( read pid && cat "/proc/$pid/maps" ; kill "$pid" )
